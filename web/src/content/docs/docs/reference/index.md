---
title: Reference
description: Exhaustive, information-oriented facts about lazybox.
---

Information-oriented, exhaustive, and dry. Look things up here; learn elsewhere.

- **[CLI](/docs/reference/cli/)** — every `lazybox` subcommand, flag, and run
  mode.
- **[Keybindings](/docs/reference/keybindings/)** — the full keymap for every
  pane and picker.
- **[Configuration](/docs/reference/configuration/)** — the complete
  `~/.lazybox/config.yaml` schema with an annotated example.
- **[JSON HTTP API](/docs/reference/json-api/)** — start the gateway,
  authenticate, and call the command and workspace endpoints.
