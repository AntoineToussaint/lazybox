WEBVTT

NOTE
"⌨ <chord> — <text>" lines render as a keycap chip + description in the
overlay; plain lines are context. Drives both the native <track> and the
styled overlay from one source. Cues must fit the clip runtime (~5.6s).

1
00:00.400 --> 00:02.100
⌨ ]]s — open the snippet picker inside the agent

2
00:02.300 --> 00:03.900
Reusable prompts, categorized, with a live body preview

3
00:04.100 --> 00:05.350
⌨ Enter — fire it into the running agent
