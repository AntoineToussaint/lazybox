WEBVTT

NOTE
"⌨ <chord> — <text>" lines render as a keycap chip + description in the
overlay; plain lines are context. Drives both the native <track> and the
styled overlay from one source. Cues must fit the clip runtime (~7.6s).

1
00:00.400 --> 00:02.600
Every task gets its own git worktree and a live agent

2
00:02.800 --> 00:05.000
Here, Codex refreshes the landing page on its own branch

3
00:05.200 --> 00:07.350
Edit, build, verify — isolated from every other agent
